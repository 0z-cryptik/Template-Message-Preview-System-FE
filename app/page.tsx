"use client";

import { Form } from "./ui/form";

const Home = () => {
  return (
    <main className="flex items-center justify-center h-screen">
      <Form />
    </main>
  );
};

export default Home;
